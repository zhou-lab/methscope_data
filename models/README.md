# Models moved to HuggingFace

The runnable model bundles are hosted on HuggingFace (too large for git):

**https://huggingface.co/zhou-lab/methscope**

```sh
# fetch one model into this folder (or drop the filename for all)
hf download zhou-lab/methscope hg38_celltype.ubjx --local-dir .
```

See the catalog and usage in the repo [`README.md`](../README.md).
